<UserControl x:Class="MCDSaveEditor.Controls.ArmorPropertyItemTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MCDSaveEditor.Controls"
             xmlns:profile="clr-namespace:MCDSaveEditor.Save.Profiles"
             xmlns:mappings="clr-namespace:MCDSaveEditor.Save.Mapping"
             xmlns:converters="clr-namespace:MCDSaveEditor.Save.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=profile:Armorproperty}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibleIfTrueConverter" />
        <converters:RarityToBulletConverter x:Key="rarityToBullet" />
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" >
        <Grid Background="{Binding Rarity, Converter={StaticResource rarityToBullet}}" Height="30" Width="30"  VerticalAlignment="Center" Margin="10,0,10,0"/>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock Margin="0,0,0,0" Text="{Binding Name, Mode=OneWay}"/>
            <TextBlock Margin="0,0,0,0" Text="{Binding Description, Mode=OneWay}" Visibility="Collapsed"/>
        </StackPanel>
    </StackPanel>
</UserControl>
